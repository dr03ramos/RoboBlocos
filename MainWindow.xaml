<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RoboBlocos.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RoboBlocos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="RoboBlocos">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="60">
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            
        <TextBlock Grid.Row="0" Text="Boas-vindas ao RoboBlocos" FontSize="28" FontWeight="SemiBold" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="600" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Left" Spacing="12" Margin="0,10,0,0">
                    <Button ToolTipService.ToolTip="Criar novo programa do zero"  Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE710;" />
                        <TextBlock Text="Novo programa" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <Button ToolTipService.ToolTip="Importar programa existente">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8B5;" />
                        <TextBlock Text="Importar programa" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <Button ToolTipService.ToolTip="Exibir área de tutoriais">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE9CE;" />
                        <TextBlock Text="Tutoriais" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch" Grid.Column="1" Margin="24,0,0,0">
                <TextBlock Text="Projetos recentes" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

                <StackPanel Spacing="4">
                    <controls:SettingsCard x:Name="settingsCard"
                               Description="Robô RCX2 | Porta serial COM3"
                               Header="Programa 1"
                               IsClickEnabled="True">
                        
                        <DropDownButton>
                            <DropDownButton.Content>
                                <TextBlock Text="Opções"/>
                            </DropDownButton.Content>
                            <DropDownButton.Flyout>
                                <MenuFlyout Placement="Bottom">
                                    <MenuFlyoutItem Text="Remover">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE711;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </DropDownButton.Flyout>
                        </DropDownButton>
                        
                        <controls:SettingsCard.HeaderIcon>
                            <SymbolIcon Symbol="Document"></SymbolIcon>
                        </controls:SettingsCard.HeaderIcon>
                        
                    </controls:SettingsCard>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
