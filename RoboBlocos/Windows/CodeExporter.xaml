<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="RoboBlocos.CodeExporter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RoboBlocos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Salvar Arquivo"
    SecondaryButtonText="Copiar Tudo"
    CloseButtonText="Fechar"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}">

    <Grid Width="800" Height="600" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Código NQC Gerado" 
                       FontSize="20" 
                       FontWeight="SemiBold" 
                       Margin="0,0,0,4"/>
            <TextBlock Text="Este é o código gerado a partir dos seus blocos. Você pode copiar ou salvar este código."
                       FontSize="12"
                       Foreground="Gray"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Code Editor -->
        <Border Grid.Row="1" 
                Background="#1E1E1E"
                CornerRadius="8"
                BorderBrush="#3C3C3C"
                BorderThickness="1"
                Margin="0,0,305,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Line Numbers -->
                <StackPanel x:Name="LineNumbersPanel"
                            Grid.Column="0"
                            Background="#141414"
                            Padding="12,12,8,12"
                            VerticalAlignment="Top"/>

                <!-- Code ScrollViewer -->
                <ScrollViewer x:Name="CodeScrollViewer"
                              Grid.Column="1"
                              HorizontalScrollMode="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollMode="Auto"
                              VerticalScrollBarVisibility="Auto"
                              Padding="12"
                              ViewChanged="CodeScrollViewer_ViewChanged">
                    <TextBlock x:Name="CodeTextBlock"
                               FontFamily="Consolas"
                               FontSize="13"
                               IsTextSelectionEnabled="True"
                               TextWrapping="NoWrap"
                               Foreground="#D4D4D4"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Info Panel -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    Spacing="8" 
                    Margin="0,0,0,8">
            <FontIcon Glyph="&#xE946;" 
                      FontSize="12" 
                      Foreground="Gray"/>
            <TextBlock x:Name="InfoTextBlock"
                       FontSize="12"
                       Foreground="Gray"/>
        </StackPanel>
    </Grid>
</ContentDialog>
